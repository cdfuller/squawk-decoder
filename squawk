#!/usr/bin/env bash
# Resolve the script location so we can call squawk_cli.py from any cwd, ignoring CDPATH quirks.
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec python "$SCRIPT_DIR/squawk_cli.py" "$@"
